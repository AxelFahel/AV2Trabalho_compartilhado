package teste;

public class main {  
   public static void main (String args[])throws Exception{  
        String numerosArray[] = new String[61];  
        int[] arrayInt = new int[61];  
        String aux;  
        try      //Abertura do tratamento de erros  
        {  
            File myFile = new File ("C:/temp/Volantes.txt"<img src="http://javafree.uol.com.br/forum/images/smiles/icon_wink.gif">;  
            FileReader my = new FileReader(myFile);  
            BufferedReader reader = new BufferedReader(my);  
            String line = null;  
            int z = 0;    
            while ((numerosArray[z] = reader.readLine()) !=null)  
            {  
                    z++;  
            }  
            reader.close();  
              
            for (int i = 0;i < 60; i++)  
            {  
                numerosArray<i> = numerosArray<i>.replace(";"," "<img src="http://javafree.uol.com.br/forum/images/smiles/icon_wink.gif">;  
                System.out.println(numerosArray<i>);  
                //arrayInt<i> = Integer.parseInt(numerosArray<i>);       
            }  
          
                         
        }catch(Exception ex)  
        {                         
            ex.printStackTrace();   
        }  
           
   }  
}  